#include<stdio.h>
int fact(int); 
int main()
{
int n,f; 
printf("Enter number\n");
scanf("%d",&n); 
f=fact(n);
printf("Factorial is %d\n",f);
}
int fact(int n)
{
int result; if (n==0)
return 0; 
else
if(n==1) return 1;
else {
result=n*fact(n-1);
return result; 
     }
}     
